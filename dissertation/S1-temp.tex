\chapter{
مفاهیم مقدماتی
}\label{S1}
\setcounter{page}{1}
\pagenumbering{arabic}

\section{مقدمه‌}

در این فصل، مفاهیم و تعاریف پایه‌ای مورد نیاز برای درک مباحث اصلی پایان‌نامه معرفی می‌شوند.
این مفاهیم شامل حساب لامبدا بدون نوع، سیستم‌های کاهش انتزاعی، حساب لامبدا نوع‌دار ساده و سیستم‌های نوع محض هستند که به ترتیب از ساده به پیچیده ارائه می‌گردند.
هدف از این فصل، ایجاد یک زبان مشترک و نمادگذاری واحد برای خواننده است تا زمینه لازم برای مطالعه فصل‌های بعدی، که به معرفی سیستم‌های نوع محض لایه‌ای، ترجمهٔ حذف وابستگی و صوری‌سازی آن‌ها در اثبات‌یار
\lr{Coq}
اختصاص دارد، فراهم آید.

فصل با بررسی حساب لامبدا بدون نوع آغاز می‌شود که پایه تاریخی و مفهومی تمام سیستم‌های بعدی را تشکیل می‌دهد.
سپس چارچوب کلی سیستم‌های کاهش انتزاعی معرفی می‌گردد که ابزار ریاضی برای مطالعه دقیق خواص نرمال‌سازی و همگرایی را فراهم می‌کند. این چارچوب به دلیل عمومیت خود، امکان بیان یکپارچه نتایج مربوط به نرمال‌سازی ضعیف، قوی و همگرایی را فراهم می‌کند.
در ادامه، حساب لامبدا نوع‌دار ساده به عنوان نخستین سیستم نوع‌دار وابسته بررسی می‌شود. این سیستم نه تنها مقدمه‌ای طبیعی برای ورود به سیستم‌های پیچیده‌تر است، بلکه نمونه‌ای کلاسیک از چگونگی جلوگیری انواع از حلقه‌های محاسباتی را نشان می‌دهد.
در نهایت، به سیستم‌های نوع محض می‌پردازیم که چارچوب کلی و یکپارچه‌ای را برای بیان طیف وسیعی از سیستم‌های نوع، از حساب لامبدا نوع‌دار ساده تا حساب ساختمان‌ها، ارائه می‌کنند.

اثبات‌های تفصیلی بسیاری از قضایا و لم‌های این فصل به منابع استاندارد مانند
\cite{barendregt} و \cite{geuvers1993}
ارجاع داده شده و تنها صورت تعاریف و قضایای کلیدی همراه با توضیحات ضروری ارائه می‌گردد.
این رویکرد از تکرار مطالب جلوگیری می‌کند و خواننده را به مطالعه منابع اصلی ترغیب می‌نماید.

با پایان این فصل، خواننده با ابزارهای نظری لازم برای درک سیستم‌های نوع محض لایه‌ای و ترجمه حذف وابستگی آشنا خواهد شد و آماده ورود به مباحث تخصصی فصل‌های بعدی می‌شود.

\section{حساب لامبدا بدون نوع}

حساب لامبدا بدون نوع که نخستین بار توسط
\lr{Alonzo Church}
در
\cite{church1932}
معرفی شد، پایه تاریخی و مفهومی تمام سیستم‌های محاسباتی تابعی و نظریه انواع مدرن به شمار می‌رود.
این دستگاه، محاسبه را به عنوان کاربرد توابع بر آرگومان‌ها مدل می‌کند و قدرت بیان معادلی با ماشین تورینگ دارد.
این حساب، با وجود سادگی نحوی، مسائل عمیقی مانند حلقه‌های نامتناهی و تصمیم‌ناپذیری را نشان می‌دهد؛ که این موارد در سیستم‌های نوع‌دار با استفاده از انواع کنترل می‌شوند.

\subsection{نحو}

\begin{defn}[نحو عبارات در حساب لامبدا بدون نوع]    
مجموعه عبارات حساب لامبدا بدون نوع، که معمولا با
$\Lambda$
نشان داده می‌شود، به صورت بازگشتی زیر تعریف می‌گردد

\begin{equation*}
M ::= V \mid \lambda V.M \mid MM
\end{equation*}

که در آن

\begin{itemize}
\item $V=\{x,y,z,\dots\}$ مجموعه متغیرهاست.
\item $\lambda V.M$ یک انتزاع و بیانگر تعریف یک تابع است.
\item $MM$ یک کاربرد و بیانگر صدا زدن یک تابع است.
\end{itemize}
\end{defn}

\subsection{متغیرهای آزاد و بسته}

برای درک دقیق مفاهیم جانشینی و کاهش، معرفی متغیرهای آزاد و بسته ضروری است.

\begin{defn}[متغیرهای آزاد یک عبارت]
برای یک عبارت
$M$،
مجموعه متغیرهای آزاد آن که با
$FV(M)$
نشان داده می‌شود، به صورت بازگشتی زیر تعریف می‌گردد
\begin{align*}
FV(x)&=\{x\}\\
FV(\lambda x.M)&=FV(M)-\{x\}\\
FV(MN)&=FV(M) \cup FV(N)
\end{align*}
\end{defn}

\begin{prop}
گزاره‌های زیر با یکدیگر معادل هستند:
\begin{itemize}
\item $x \in FV(M)$.
\item متغیر $x$ در عبارت $M$ آزاد است.
\item متغیر $x$ در عبارت $M$ دارای رخداد آزاد است.
\end{itemize}
\end{prop}

\begin{prop}
گزاره‌های زیر با یکدیگر معادل هستند:
\begin{itemize}
\item $x \notin FV(M)$.
\item متغیر $x$ در عبارت $M$ بسته است.
\item متغیر $x$ در عبارت $M$ دارای رخداد بسته است.
\end{itemize}
\end{prop}

\begin{prop}
گزاره‌های زیر با یکدیگر معادل هستند:
\begin{itemize}
\item $FV(M) = \emptyset$.
\item عبارت $M$ بسته است.
\item عبارت $M$ یک ترکیب‌گر است.
\end{itemize}
\end{prop}

\subsection{جانشینی}

جانشینی با نماد
$M[N/x]$
نشان داده می‌شود و معنای آن جایگزینی همه رخدادهای آزاد
$x$
در
$M$
با عبارت
$N$
است.

\begin{rem}
در برخی منابع مانند
\cite{barendregt}،
جانشینی با نماد
\begin{equation*}
M[x:=N]
\end{equation*}
نشان داده می‌شود. ما در این پایان‌نامه از نماد متداول دیگری استفاده می‌کنیم که
\lr{Mull}
نیز در تز دکتری خود استفاده کرده است، یعنی
\begin{equation*}
M[N/x]   
\end{equation*}
\end{rem}

\begin{defn}
جانشینی به صورت بازگشتی زیر تعریف می‌شود
\begin{align*}
x[N/x] &= N\\
y[N/x] &= y & (x \neq y) \\
PQ[N/x]&=(P[N/x])(Q[N/x])\\
(\lambda x.M)[N/x] &= \lambda x.M\\
(\lambda y.M)[N/x] &= \lambda y.(M[N/x]) & (x \neq y)
\end{align*}
\end{defn}

\subsection{تبدیل $\alpha$}

تبدیل
$\alpha$
به ما اجازه می‌دهد نام متغیرهای بسته را، بدون تغییر معنای عبارت، تغییر دهیم.

\begin{defn}
اگر
$M$
یک عبارت باشد و
$x \notin FV(M)$،
آنگاه تبدیل
$\alpha$
به صورت زیر بیان می‌شود
\begin{equation*}
\lambda x.M \equiv_{\alpha} \lambda y.M[y/x]
\end{equation*}
\end{defn}

\subsection{کاهش $\beta$}

\begin{defn}
کاهش
$\beta$
قاعده اصلی محاسبه در حساب لامبدا است و به صورت زیر تعریف می‌شود
\begin{equation*}
(\lambda x.M)N \rightarrow_{\beta} M[N/x]
\end{equation*}
که بستار بازتاب-تعدی آن با نماد
$\twoheadrightarrow_{\beta}$
نشان داده می‌شود.
\end{defn}

\subsubsection{مثال}
\begin{enumerate}
\item
\begin{align*}
\lambda x.(\lambda xy.y)x &\equiv_{\alpha} \lambda x.(\lambda ry.y)x\\
&\rightarrow_{\beta} \lambda x.(\lambda y.y)[x/r]\\
&= \lambda x.(\lambda y.y)\\
\end{align*}
\item \label{infinite_reduction} اگر $M \equiv \lambda x.xx$، آنگاه
\begin{align*}
MM &\equiv (\lambda x.xx)M\\
&\rightarrow_{\beta} xx[M/x]\\
&= MM\\
&\equiv (\lambda x.xx)M\\
&\rightarrow_{\beta} xx[M/x]\\
&= \dots
\end{align*}
پس داریم
\begin{equation*}
MM \rightarrow_{\beta} MM \rightarrow_{\beta} MM \rightarrow_{\beta} \dots
\end{equation*}
\end{enumerate}

همانطور که در مثال ۲ مشاهده می‌کنید، دنباله کاهش عبارت $MM$ نامتناهی است. این مثال ما را به مفاهیم بعدی سوق می‌دهد.

\subsection{مسیرهای کاهش و فرم نرمال}\label{normalization}

\begin{defn}
یک مسیر کاهش، دنباله‌ای به شکل زیر است که می‌تواند متناهی یا نامتناهی باشد.
\begin{equation*}
M_0 \rightarrow_{\beta} M_1 \rightarrow_{\beta} \dots \rightarrow_{\beta} M_n
\end{equation*}
\end{defn}

\begin{defn}
عبارت
$M$
در فرم نرمال (یا یک فرم نرمال) است، اگر هیچ گامی از کاهش
$\beta$
بر آن قابل اعمال نباشد.
یعنی
\begin{equation*}
M \in \mathrm{NF} \iff \nexists N. M \rightarrow_{\beta} N
\end{equation*}
\end{defn}

\subsubsection{مثال}
عبارات زیر در فرم نرمال هستند:
\begin{align*}
&x\\
&x(\lambda a.a)\\
&\lambda x.x\\
&\lambda y.x
\end{align*}

عبارات زیر در فرم نرمال نیستند:
\begin{align*}
&(\lambda x.x)x\\
&(\lambda y.xy)(\lambda z.r)
\end{align*}

زیرا به ترتیب داریم:
\begin{align*}
&(\lambda x.x)x \equiv_{\alpha} (\lambda y.y)x \rightarrow_{\beta} y[x/y] = x\\
&(\lambda y.xy)(\lambda z.r) \rightarrow_{\beta} xy[(\lambda z.r)/y] = x(\lambda z.r)
\end{align*}

\subsection{نرمال‌سازی ضعیف و قوی}

\begin{defn}[نرمال‌سازی ضعیف]
عبارت
$M$
دارای خاصیت نرمال‌سازی ضعیف است، اگر حداقل یک مسیر کاهش به یک فرم نرمال داشته باشد.
یعنی
\begin{equation*}
\exists N\in \mathrm{NF}.\;M \twoheadrightarrow_{\beta} N
\end{equation*}
\end{defn}

\begin{defn}[نرمال‌سازی قوی]
عبارت
$M$
دارای خاصیت نرمال‌سازی قوی است، اگر هیچ مسیر کاهش نامتناهی‌ای از آن آغاز نشود.
\end{defn}

\begin{thm}
اگر یک عبارت دارای خاصیت نرمال‌سازی قوی باشد، آنگاه دارای خاصیت نرمال‌سازی ضعیف نیز است.
\end{thm}

عکس قضیه فوق همان حدس
\lr{BGK}
است که برای سیستم‌های وابسته، هنوز اثبات نشده است.

\begin{conj}[\lr{BGK}]\label{BGK}
اگر یک عبارت دارای خاصیت نرمال‌سازی ضعیف باشد، آنگاه دارای خاصیت نرمال‌سازی قوی نیز است.
\end{conj}

\begin{thm}
در حساب لامبدا بدون نوع، نرمال‌سازی ضعیف و قوی معادل‌ هستند؛ به بیان دیگر، حدس
\ref{BGK}
در حساب لامبدا بدون نوع، برقرار است.
\end{thm}

\subsubsection{مثال}
همانطور که در
\ref{infinite_reduction}
دیدیم، عبارت زیر دارای خاصیت نرمال‌سازی قوی نیست
\begin{equation*}
(\lambda x.xx)(\lambda x.xx)
\end{equation*}

\section{سیستم‌های کاهش انتزاعی}

سیستم‌های کاهش انتزاعی\footnote{\lr{Abstract Reduction Systems} یا به اختصار \lr{ARS}}
که نخستین بار توسط
\lr{Max Newman}
در
\cite{newman1942}
به منظور مطالعه همگرایی روابط کاهش معرفی شدند، چارچوبی ریاضی برای مطالعه روابط کاهش در سیستم‌های محاسباتی هستند.
بسیاری از مفاهیم نظریه انواع
(از جمله نرمال‌سازی ضعیف و قوی، همگرایی و یکتایی فرم نرمال)
به‌طور ضمنی در این چارچوب بیان می‌شوند.
در این بخش تعریف رسمی و ویژگی‌های بنیادی این چارچوب را بیان می‌کنیم.

\subsection{تعریف سیستم کاهش انتزاعی}

\begin{defn}
یک سیستم کاهش انتزاعی، یک زوج
$(A,\rightarrow)$
است که در آن:
\begin{itemize}
\item 
$A$
مجموعه‌ اشیا است.
\item
$\rightarrow \; \subseteq A \times A$
یک رابطه دودویی روی
$A$
است که رابطه کاهش نام دارد.
\end{itemize}
\end{defn}

برای مثال در حساب لامبدا، مجموع عبارات
$\Lambda$
همراه با رابطه کاهش
$\beta$،
یک
ARS
را تشکیل می‌دهند.

\subsection{دنباله و بستار کاهش}

\begin{defn}
دنباله کاهش، زنجیره‌ای از کاهش‌ها به شکل زیر است که می‌تواند متناهی یا نامتناهی باشد.
\begin{equation*}
a_0 \rightarrow a_1 \rightarrow \dots \rightarrow a_n   
\end{equation*}
\end{defn}

\begin{defn}
بستار تعدی رابطه
$\rightarrow$
را با
$b \rightarrow^+ a$
نشان می‌دهیم که به این معناست که حداقل یک گام کاهش از
$b$
به
$a$
وجود دارد.

بستار بازتابی-تعدی رابطه
$\rightarrow$
را با
$b \rightarrow^* a$
نشان می‌دهیم که یعنی
$b = a$
یا
یک دنباله کاهش از
$b$
به
$a$
وجود دارد.
\end{defn}

\subsection{فرم نرمال و نرمال‌سازی}

\begin{defn}[فرم نرمال]
شی
$a$
در فرم نرمال (یا یک فرم نرمال) است اگر هیچ کاهشی بر آن قابل اعمال نباشد. یعنی
\begin{equation*}
a \in \mathrm{NF} \iff \nexists b.\;a \rightarrow b
\end{equation*}
\end{defn}

\begin{defn}[نرمال‌سازی ضعیف]
شی
$a$
دارای خاصیت نرمال‌سازی ضعیف است، اگر حداقل یک دنباله کاهش به یک فرم نرمال داشته باشد. یعنی
\begin{equation*}
\exists b \in \mathrm{NF}.\;a \rightarrow^* b   
\end{equation*}
\end{defn}

\begin{defn}[نرمال‌سازی قوی]
شی
$a$
دارای خاصیت نرمال‌سازی قوی است، اگر هیچ دنباله کاهش نامتناهی از آن آغاز نشود.
\end{defn}

\begin{thm}
اگر یک شی دارای خاصیت نرمال‌سازی قوی باشد، آنگاه دارای خاصیت نرمال‌سازی ضعیف نیز است.
\end{thm}

\subsection{همگرایی}

\begin{defn}[همگرایی]
سیستم کاهش انتزاعی
$(A,\rightarrow)$
همگراست، هرگاه هر دو دنباله کاهش که از یک شی منشعب می‌شوند، دوباره به شی مشترکی برسند. یعنی
\begin{equation*}
\forall a,b_1,b_2.\;(a \rightarrow^* b_1 \land a \rightarrow^* b_2) \implies (\exists c.\; (b_1 \rightarrow^* c \land b_2 \rightarrow^* c))
\end{equation*}
\end{defn}

نتایجی مانند یکتایی فرم نرمال از قضیه پیش رو ثابت می‌شوند.

\begin{thm}[قضیه چرچ-راسر]
سیستم کاهش انتزاعی
$(A,\rightarrow)$
همگراست، اگر و تنها اگر دارای خاصیت چرچ-راسر باشد؛ یعنی
\begin{equation*}
\forall b_1,b_2.\;(b_1 = b_2) \implies (\exists c.\; (b_1 \rightarrow^* c \land b_2 \rightarrow^* c))
\end{equation*}
\end{thm}

\begin{cor}[یکتایی فرم نرمال]
فرم نرمال یک شی، در صورت وجود، یکتاست. یعنی
\begin{equation*}
\forall M, N_1 \in \mathrm{NF}, N_2 \in \mathrm{NF}.\;(M \rightarrow^* N_1 \land M \rightarrow^* N_2 ) \implies N_1 = N_2
\end{equation*}
\end{cor}

اثبات همگرایی معمولاً دشوار است، اما یک ویژگی محلی ساده‌تر وجود دارد.

\begin{defn}[همگرایی محلی]
سیستم کاهش انتزاعی
$(A,\rightarrow)$
همگرای محلی است اگر
\begin{equation*}
\forall a,b_1,b_2.\;(a \rightarrow b_1 \land a \rightarrow b_2) \implies (\exists c.\; (b_1 \rightarrow^* c \land b_2 \rightarrow^* c))
\end{equation*}
\end{defn}

\begin{lmm}[لم نیومن]
اگر یک سیستم کاهش انتزاعی، همگرای محلی و دارای خاصیت نرمال‌سازی قوی باشد، آنگاه همگراست.
\end{lmm}

\begin{rem}
در برخی سیستم‌ها
(مانند حساب لامبدا بدون نوع)،
به دلیل همگرایی، نرمال‌سازی ضعیف معادل نرمال‌سازی قوی است. اما در سیستم‌های نوع‌دار وابسته، ممکن است عبارتی دارای خاصیت نرمال‌سازی ضعیف باشد اما در حداقل یک دنباله کاهش نامتناهی ظاهر شود. این دقیقا مسئله‌ای است که حدس
\ref{BGK}
به آن می‌پردازد و
\lr{Mull}
در
\cite{mull2023}
با ارائه سیستم‌های نوع محض لایه‌ای می‌کوشد که آن را حل کند.
\end{rem}

\section{حساب لامبدا نوع‌دار ساده}

حساب لامبدا نوع‌دار ساده\footnote{\lr{Simply Typed Lambda Calculus} یا به اختصار \lr{STLC} یا $\lambda \to$}
که نخستین بار توسط
\lr{Alonzo Church}
در
\cite{church1940}
به عنوان پایه‌ای برای منطق معرفی شد، یکی از مهم‌ترین و کلاسیک‌ترین سیستم‌های نوع است.
این سیستم پایه بسیاری از زبان‌های تابعی، سیستم‌های نوع پیشرفته و بخش مهمی از نظریه انواع به شمار می‌رود.
همچنین ساده‌ترین نمونه از «سیستم‌های نوع محض» است و مقدمه‌ای طبیعی برای ورود به مباحث بخش بعدی به حساب می‌آید.

سیستم مذکور نشان می‌دهد که افزودن انواع، باعث تضمین نرمال‌سازی قوی می‌شود و از کاهش‌های نامتناهی جلوگیری می‌کند.

\subsection{نحو}

\begin{defn}[انواع]
مجموعه انواع به صورت بازگشتی زیر تعریف می‌شود
\begin{equation*}
T ::= B \mid T \to T
\end{equation*}
که در آن
\begin{itemize}
\item $B=\{\alpha,\beta,\gamma,\dots\}$ مجموعه انواع پایه است.
\item $T \to T$ بیانگر نوع توابع است.
\end{itemize}
\end{defn}

\begin{defn}[عبارات]
مجموعه عبارات به صورت بازگشتی زیر تعریف می‌شود
\begin{equation*}
M ::= V \mid \lambda V^T.M \mid MM
\end{equation*}
که در آن
\begin{itemize}
\item $V=\{x,y,z,\dots\}$ مجموعه متغیرهاست.
\item $\lambda V^T.M$ یک انتزاع و بیانگر تعریف یک تابع است.
\item $MM$ یک کاربرد و بیانگر صدا زدن یک تابع است.
\end{itemize}
\end{defn}

\subsection{زمینه و قضاوت نوع}

\begin{defn}[زمینه]
یک زمینه مانند
$\Gamma$،
دنباله‌ای متناهی از مفروضات به شکل زیر است
\begin{equation*}
x_1:A_1,\dots,x_n:A_n    
\end{equation*}
که
\begin{itemize}
\item $x_i:A_i$ به این معناست که $x_i$ از نوع $A_i$ است.
\item $\Gamma,x:A$ به معنای افزودن فرض $x:A$ به $\Gamma$ است.
\end{itemize}
\end{defn}

\begin{defn}[قضاوت نوع]
یک قضاوت نوع، گزاره‌ای به شکل زیر است
\begin{equation*}
\Gamma \vdash M:A
\end{equation*}
که در آن
$\Gamma$
یک زمینه،
$M$
یک عبارت و
$A$
یک نوع است و معنای آن این است که
"در زمینه
$\Gamma$،
عبارت
$M$
از نوع
$A$
است".
\end{defn}

\begin{defn}[قواعد قضاوت نوع]\label{type_rules_1}
قواعد قضاوت نوع در
$STLC$
شامل سه قاعده زیر است:

\begin{itemize}
\item قاعده متغیر
\begin{prooftree}
\AxiomC{}
\UnaryInfC{$\Gamma,x:A \vdash x:A$}
\end{prooftree}
\item قاعده انتزاع
\begin{prooftree}
\AxiomC{$\Gamma,x:A \vdash M:B$}
\UnaryInfC{$\Gamma \vdash \lambda x^A.M:A \to B$}
\end{prooftree}
\item قاعده کاربرد
\begin{prooftree}
\AxiomC{$\Gamma \vdash N:A$}
\AxiomC{$\Gamma \vdash M:A \to B$}
\BinaryInfC{$\Gamma \vdash MB:B$}
\end{prooftree}
\end{itemize}
\end{defn}

\begin{defn}[نوع‌پذیری]
یک عبارت نوع‌پذیر است هرگاه بتوان طبق قواعد
\ref{type_rules_1}
یک نوع برای آن یافت.

به بیان دیگر؛ عبارت
$M$
نوع‌پذیر است هرگاه
\begin{equation*}
\exists \Gamma, A.\; \Gamma \vdash M : A
\end{equation*}
\end{defn}

\subsubsection{مثال}
عبارات زیر نوع‌پذیر هستند؛

\begin{align*}
\lambda x&.x \\
\lambda xy&.x \\
\lambda fgx&.(f(gx))
\end{align*}

زیرا به ترتیب داریم؛

\begin{align*}
\lambda x^A.x &: A \to A\\
\lambda x^Ay^B.x &: A \to B \to A\\
\lambda f^{B \to C}g^{A \to B}x^A.(f(gx)) &: (B \to C) \to (A \to B) \to A \to C
\end{align*}

عبارت زیر، که قبل‌تر در
\ref{infinite_reduction}
دیدیم، در
\lr{STLC}
نوع‌پذیر نیست؛
\begin{align*}
(\lambda x.(xx))(\lambda x.(xx)) \\
\end{align*}

مثال فوق ما را به سمت قضیه مهم بعدی سوق می‌دهد.

\subsection{خواص}
در این بخش، برخی خواص مهم
\lr{STLC}
را مطالعه می‌کنیم.

\begin{thm}[حفظ نوع در کاهش]
کاهش دادن یک عبارت، نوع آن را تغییر نمی‌دهد. یعنی
\begin{equation*}
(\Gamma \vdash M : A \land M \rightarrow_\beta N) \implies \Gamma \vdash N : A
\end{equation*}
\end{thm}

قضیه بعدی بیان می‌کند که وجود انواع، از ایجاد عباراتی که دارای خاصیت نرمال‌سازی قوی نیستند، جلوگیری می‌کند.

\begin{thm}[قضیه نرمال‌سازی قوی]
هر عبارت نوع‌پذیر در
\lr{STLC}
دارای خاصیت نرمال‌سازی قوی است.
\end{thm}

\section{سیستم‌های نوع محض}

سیستم‌های نوع محض\footnote{\lr{Pure Type Systems} یا به اختصار \lr{PTS}}
که نخستین بار توسط
\cite{berardi1988} و \cite{terlouw1989}
معرفی شدند و سپس توسط
\cite{barendregt1991}
تعمیم یافتند، یک چارچوب یکپارچه برای توصیف طیف وسیعی از سیستم‌های نوع مبتنی بر حساب لامبدا را فراهم می‌کنند.
بسیاری از سیستم‌های مهم از جمله حساب لامبدا نوع‌دار ساده، سیستم‌های چندریخت و حساب ساختمان‌ها را می‌توان تنها با انتخاب مناسب مجموعه‌ای از "رده‌ها"، "اصول" و "روابط" در قالب یک
\lr{PTS}
بیان کرد. این چارچوب به دلیل سادگی نحوی و هم‌چنین انعطاف بسیار بالا، نقش محوری در نظریه انواع دارد. از طرفی سیستمی که
\lr{Mull}
در \cite{mull2023}
معرفی می‌کند که اساس کار این پایان‌نامه است، نسخه‌ تغییر یافته همین سیستم است؛
لذا آشنایی با این سیستم برای ما ضروری است.

\subsection{مشخصه و نحو}

\begin{defn}[مشخصه سیستم نوع محض]
یک سیستم نوع محض با سه تایی
$(\mathcal{S},\mathcal{A},\mathcal{R})$
مشخص می‌شود که در آن
\begin{itemize}
\item $S=\{s_1,s_2,\dots\}$ مجموعه رده‌ها
\item $A \subseteq S \times S$ مجموعه اصول
\item $R \subseteq S \times S \times S$ مجموعه روابط
\end{itemize}
هستند.
\end{defn}

عبارات و انواع در
\lr{PTS}
از منظر نحو یکسان هستند؛یعنی در این چارچوب، "نوع" و "عبارت" یک ساختار واحد دارند و فقط جایگاه آن‌ها در قضاوت نوع است که نقش متفاوتی به آن‌ها می‌دهد.

\begin{defn}[نحو سیستم نوع محض]
نحو سیستم نوع محض به صورت بازگشتی زیر تعریف می‌شود
\begin{equation*}
T ::= \mathcal{S} \mid V \mid \lambda V:T.T \mid \Pi V:T.T \mid TT
\end{equation*}
که در آن
\begin{itemize}
\item $\mathcal{S}=\{s_1,s_2,\dots\}$ مجموعه رده‌ها
\item $V=\{x,y,z,\dots\}$ مجموعه متغیرها
\item $\lambda V:T.T$ انتزاع
\item $\Pi V:T.T$ نوع تابع
\item $TT$ کاربرد
\end{itemize}
هستند.
\end{defn}

در این پایان‌نامه، مطابق با
\cite{mull2023}،
متغیرها را با رده خودشان نشانه‌گذاری می‌شوند.

\begin{defn}[نشانه گذاری متغیرها]
برای هر
$s \in \mathcal{S}$، 
$V_s$
را مجموعه شمارشی متغیرهای مربوط به رده
$s$
در نظر می‌گیریم.

$^sv_i$
به معنای
$i$
مین متغیر در
$V_s$
است و
$V = \bigcup_{s \in \mathcal{S}}V_s$.
\end{defn}

\subsection{قضاوت نوع}

\begin{defn}[زمینه]
یک زمینه مانند
$\Gamma$،
دنباله‌ای متناهی از مفروضات به شکل زیر است
\begin{equation*}
x_1:A_1,\dots,x_n:A_n    
\end{equation*}
که در آن
$x_i:A_i$
به این معناست که
"$x_i$
از نوع
$A_i$
است".
همچنین
$\Gamma,x:A$
به معنای افزودن فرض
$x:A$
به
$\Gamma$
است.
\end{defn}

\begin{defn}[قواعد قضاوت نوع در سیستم نوع محض]\label{pts_rules}
در یک سیستم نوع محض با مشخصه
$(\mathcal{S},\mathcal{A},\mathcal{R})$
قواعد زیر موجود است:

\begin{itemize}
\item قاصده اصل
\begin{prooftree}
\AxiomC{}
\LeftLabel{$\quad (s,t) \in \mathcal{A}$}
\UnaryInfC{$\vdash s:t$}
\end{prooftree}
\item قاعده شروع
\begin{prooftree}
\AxiomC{$\Gamma \vdash A:s$}
\LeftLabel{$\quad ^sx \notin \Gamma$}
\UnaryInfC{$\Gamma,^sx:A \vdash ^sx:A$}
\end{prooftree}
\item قاعده تضعیف
\begin{prooftree}
\AxiomC{$\Gamma \vdash B:s$}
\AxiomC{$\Gamma \vdash M:A$}
\LeftLabel{$\quad ^sx \notin \Gamma$}
\BinaryInfC{$\Gamma,^sx:B \vdash M:A$}
\end{prooftree}
\item قاعده تولید
\begin{prooftree}
\AxiomC{$\Gamma,^{s_1}x:A \vdash B:s_2$}
\AxiomC{$\Gamma \vdash A:s_1$}
\LeftLabel{$\quad (s_1,s_2,s_3) \in \mathcal{R}$}
\BinaryInfC{$\Gamma \vdash (\Pi ^{s_1}x:A.B):s_3$}
\end{prooftree}
\item قاعده انتزاع
\begin{prooftree}
\AxiomC{$\Gamma \vdash \Pi ^sx:A.B:s$}
\AxiomC{$\Gamma,^sx:A \vdash M:B$}
\BinaryInfC{$\Gamma \vdash (\lambda ^sx:A.M):(\Pi ^sx:A.B)$}
\end{prooftree}
\item قاعده کاربرد
\begin{prooftree}
\AxiomC{$\Gamma \vdash N:A$}
\AxiomC{$\Gamma \vdash M:(\Pi ^sx:A.B)$}
\BinaryInfC{$\Gamma \vdash MN:B[N/^sx]$}
\end{prooftree}
\item قاعده تبدیل
\begin{prooftree}
\AxiomC{$\Gamma \vdash B:s$}
\AxiomC{$A \equiv_\beta B$}
\AxiomC{$\Gamma \vdash M:A$}
\TrinaryInfC{$\Gamma \vdash M:B$}
\end{prooftree}
\end{itemize}
\end{defn}

\subsection{مکعب لامبدا}

\subsubsection{توصیف}

مکعب لامبدا یک چارچوب مفهومی و هندسی برای دسته‌بندی خانواده‌ای از سیستم‌های نوع است. این مکعب نشان می‌دهد که چگونه با افزودن یا حذف برخی قابلیت‌های نوعی، می‌توان به سیستم‌های مختلفی از حساب لامبدا دست یافت.

\begin{figure}[h!]
\centering
\includegraphics[scale=0.2]{lambda_cube.png}
\caption{مکعب لامبدا}
\label{fig.lambda_cube}
\end{figure}

مکعب لامبدا دارای سه بُعد مستقل است که هر بُعد متناظر با افزودن یک قابلیت به سیستم پایه است:

\begin{enumerate}
\item \textbf{چندریختی}\\
این بُعد نشان‌دهنده امکان کمی‌سازی روی انواع است؛ به بیان دیگر، اجازه می‌دهد توابعی تعریف شوند که برای همه انواع معتبر باشند.
وجود این قابلیت منجر به سیستم‌هایی مانند
$\lambda2$
می‌شود.
\item \textbf{اپراتورهای نوع}\\
این بُعد امکان تعریف توابعی را فراهم می‌کند که روی انواع عمل می‌کنند، نه روی عبارات. در این حالت، انواع می‌توانند به‌عنوان ورودی توابع به‌کار روند و این امر منجر به سیستم‌هایی مانند
$\lambda \omega$
می‌شود.
\item \textbf{انواع وابسته}\\
این بُعد اجازه می‌دهد نوع یک عبارت به مقدار یک عبارت دیگر وابسته باشد. به این ترتیب می‌توان انواعی تعریف کرد که اطلاعاتی درباره مقدار عبارات در خود جای می‌دهند؛ این قابلیت، پایه سیستم‌هایی مانند حساب ساختمان‌هاست.
\end{enumerate}

ترکیب یا عدم ترکیب هر یک از این سه قابلیت، هشت رأس مکعب را تشکیل می‌دهد که هر رأس متناظر با یک سیستم نوع مشهور است. برای مثال، حساب لامبدای نوع‌دار ساده در رأسی قرار دارد که هیچ‌یک از این قابلیت‌ها را ندارد، در حالی که حساب ساختمان‌ها در رأسی قرار می‌گیرد که هر سه قابلیت را به‌طور هم‌زمان داراست.

\subsubsection{تعریف به کمک سیستم‌های نوع محض}

حال با معرفی کامل سیستم‌های نوع محض، بسیاری از سیستم‌های مشهور تنها با انتخاب مناسب
$(\mathcal{S},\mathcal{A},\mathcal{R})$
قابل بیان‌اند. جدول
\ref{lambda_cube_pts}
بیانگر تعریف هر هشت راس مکعب لامبدا به کمک سیستم‌های نوع محض است.

\begin{table}[h!]
\centering
\begin{tabular}{c|c|c|c}
%\hline
$\mathcal{R}$&$\mathcal{A}$&$\mathcal{S}$&\\[0.1cm]
\hline\hline
$\{(*,*,*)\}$&$\{(*:\Box)\}$&$\{(*,\Box)\}$&$\lambda \to$\\[0.3cm]
$\{(*,*,*),(\Box,*,*)\}$&$\{(*:\Box)\}$&$\{(*,\Box)\}$&$\lambda 2$\\[0.3cm]
$\{(*,*,*),(*,\Box,\Box)\}$&$\{(*:\Box)\}$&$\{(*,\Box)\}$&$\lambda \underline{\omega}$\\[0.3cm]
$\{(*,*,*),(\Box,\Box,\Box)\}$&$\{(*:\Box)\}$&$\{(*,\Box)\}$&$\lambda P$\\[0.3cm]
$\{(*,*,*),(\Box,\Box,\Box),(\Box,*,*)\}$&$\{(*:\Box)\}$&$\{(*,\Box)\}$&$\lambda P2$\\[0.3cm]
$\{(*,*,*),(*,\Box,\Box),(\Box,*,*)\}$&$\{(*:\Box)\}$&$\{(*,\Box)\}$&$\lambda \omega$\\[0.3cm]
$\{(*,*,*),(\Box,\Box,\Box),(*,\Box,\Box)\}$&$\{(*:\Box)\}$&$\{(*,\Box)\}$&$\lambda P\underline{\omega}$\\[0.3cm]
$\{(*,*,*),(\Box,\Box,\Box),(*,\Box,\Box),(\Box,*,*)\}$&$\{(*:\Box)\}$&$\{(*,\Box)\}$&$\lambda C$\\[0.3cm]
\end{tabular}
\caption{جدول بیان سیستم‌های نوع در مکعب لامبدا به کمک سیستم‌های نوع محض}
\label{lambda_cube_pts}
\end{table}


\subsection{اهمیت}

در ادامه این پایان‌نامه، سیستم‌های نوع محض نقطهٔ آغاز برای تعریف «سیستم‌های نوع محض لایه‌ای\footnote{\lr{Tiered Pure Type Systems} یا \lr{TPTS}}» 
سپس معرفی ترجمهٔ حذف وابستگی \lr{Mull} هستند.
در واقع:
\begin{itemize}
\item \lr{TPTS} گسترشی ساختاریافته از \lr{PTS} است.
\item قواعد نوع \lr{TPTS} نسخه تقویت‌شده همین قواعد‌اند.
\item اثبات‌های نرمال‌سازی وابسته به ساختار \lr{PTS} بیان می‌شوند.
\end{itemize}
در نتیجه، این بخش چارچوب مفهومی لازم برای تعریف سیستم‌های لایه‌ای و صوری‌سازی آن‌ها در \lr{Coq} را فراهم می‌کند.

\subsection{خواص فرانظری سیستم‌های نوع محض}

در این بخش برخی خواص فرانظری سیستم‌های نوع محض را مورد بررسی قرار می‌دهیم.

\begin{rem}
از تکرار تعاریفی که قبلا مشاهده کرده‌ایم
(برای مثال فرم نرمال و نرمال‌سازی برای یک عبارت که در
\ref{normalization}
دیدیم)
خودداری می‌کنیم.
\end{rem}

\begin{defn}[نرمال‌سازی ضعیف و قوی در سیستم]
یک سیستم نوع محض دارای خاصیت نرمال‌سازی ضعیف یا قوی است، هرگاه هر عبارت نوع‌پذیر در آن دارای این خاصیت باشد.
\end{defn}

\begin{lmm}[تولید]
\hfill
\begin{itemize}

\item \emph{رده:}
\begin{equation*}
\forall s \in \mathcal{S}.\quad \Gamma \vdash s : A \implies \exists s' \in \mathcal{S}.\; (A =_\beta s') \land ((s,s') \in \mathcal{A})    
\end{equation*}

\item \emph{متغیر:}
\begin{equation*}
\forall {}^sx \in V.\quad \Gamma \vdash {}^sx : A \implies \exists B.\; (A =_\beta B) \land ((^sx:B) \in \Gamma)
\end{equation*}

\item \emph{عبارت $\Pi$:}
\begin{align*}
\forall s \in \mathcal{S},B,C.\quad &\Gamma \vdash (\Pi ^sx:B.C):A \implies\\ 
&\exists s',s'' \in \mathcal{S}.\; (\Gamma \vdash B:s) \land (\Gamma,^sx:B \vdash C:s') \land ((s,s',s'') \in \mathcal{R}) \land (A =_\beta s'')
\end{align*}

\item \emph{عبارت $\lambda$:}
\begin{align*}
\forall s \in \mathcal{S},B,M.\quad &\Gamma \vdash (\lambda ^sx:B.M):A \implies\\ 
&\exists s' \in \mathcal{S},C.\; (\Gamma \vdash (\Pi ^sx:B.C):s') \land (\Gamma,^sx:B \vdash M:C) \land (A =_\beta \Pi ^sx:B.C)
\end{align*}

\item \emph{کاربرد:}
\begin{align*}
\forall M,N.\quad &\Gamma \vdash MN:A \implies\\ 
&\exists s \in \mathcal{S},B,C.\; (\Gamma \vdash M:(\Pi ^sx:B.C)) \land (\Gamma \vdash N:B) \land (A =_\beta C[N/{}^sx])
\end{align*}

\end{itemize}
\end{lmm}

\begin{lmm}[جانشینی]
\begin{align*}
\forall \Gamma,\Delta,M,N,A,B.\quad (\Gamma, x:A, \Delta \vdash M:B) \land (\Gamma \vdash N:A) \implies \Gamma, \Delta[N/x] \vdash M[N/x] : B[N/x]
\end{align*}
\end{lmm}

\begin{lmm}[صحت نوع]
\begin{equation*}
\forall \Gamma,M,A.\quad \Gamma \vdash M:A \implies (A \in \mathcal{S}) \lor (\exists s \in \mathcal{S}.\; \Gamma \vdash A:s)
\end{equation*}
\end{lmm}

\begin{lmm}[تضعیف]
\begin{equation*}
\forall \Gamma,\Delta,M,A.\quad (\Gamma \subset \Delta) \land (\Gamma \vdash M : A) \implies \Delta \vdash M : A
\end{equation*}
\end{lmm}

\begin{lmm}[جابه‌جایی]
\begin{align*}
\forall \Gamma,\Delta,x,y,A,B,C,M.\quad (x \notin FV(B)) \land (&\Gamma,x:A,y:B,\Delta \vdash M:C) \\
\implies &\Gamma,y:B,x:A,\Delta \vdash M:C
\end{align*}
\end{lmm}

\begin{defn}[سیستم نوع محض تابعی]
یک سیستم نوع محض  را تابعی گوییم، هرگاه
\begin{itemize}
\item اگر $(s,t) \in \mathcal{A}$ و $(s,t') \in \mathcal{A}$ آنگاه $t=t'$.
\item اگر $(s,t,u) \in \mathcal{R}$ و $(s,t,u') \in \mathcal{A}$ آنگاه $u=u'$.
\end{itemize}
\end{defn}

\begin{lmm}[یکتایی نوع]
در یک سیستم نوع محض تابعی، نوع هر عبارت نوع‌پذیر، یکتاست. یعنی
\begin{equation*}
\forall \Gamma, M, A, B.\quad (\Gamma \vdash M : A) \land (\Gamma \vdash M : B) \implies A =_\beta B
\end{equation*}
\end{lmm}

\begin{defn}[رده‌های بالا و پایین]
\hfill
\begin{itemize}
\item رده
$s$
یک رده بالا است هرگاه
\begin{equation*}
\nexists s' \in \mathcal{S}.\; (s,s') \in \mathcal{A}
\end{equation*}
\item رده
$s$
یک رده پایین است هرگاه
\begin{equation*}
\nexists s' \in \mathcal{S}.\; (s',s) \in \mathcal{A}
\end{equation*}
\end{itemize}
\end{defn}

\begin{lmm}[رده بالا]
برای هر زمینه
$\Gamma$،
متغیر
$x$،
عبارات
$A$ و $B$
و رده بالا
$s$،
گزاره‌های زیر برقرار هستند:
\begin{align*}
&\Gamma \nvdash s:A\\
&\Gamma \nvdash x:s\\
&\Gamma \nvdash AB:s
\end{align*}
\end{lmm}
