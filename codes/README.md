# Tiered PTS Formalization

This project is a Coq/Rocq formalization of Pure Type Systems (PTS) and Tiered Pure Type Systems.

## Building the Project

First, generate the Makefile from the `_CoqProject` configuration:

```bash
rocq makefile -f _CoqProject -o Makefile
```

Then compile the project:

```bash
make
```
